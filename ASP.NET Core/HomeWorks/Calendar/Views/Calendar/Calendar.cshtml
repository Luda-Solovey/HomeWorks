@using DependencyInjHW.Services
@model string[];

<body>
  @*   <ul>
        @for(int i = 0; i < Model.Length; i++)
        {
            <li>@Model[i];</li>
        }
    </ul> *@

    <ul>
        @foreach (var item in Model)
        {
            <li>@item</li>
        }
    </ul>
</body>

